// Change FROM this:
datasource db {
  provider  = "postgresql"
  url       = env("NETLIFY_DATABASE_URL")
  directUrl = env("NETLIFY_DATABASE_URL_UNPOOLED")
}

// Change TO this:
datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
}